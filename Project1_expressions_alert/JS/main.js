var X = `String here... \"It's a strange one\", but it's not too weird, right?`; //Created a variable called X with a value of that long string

window.alert(X); //This brings a pop-up window when the page loads, printing X

var Y = "John" + " Michael" //Creates a variable called Y with string value "John Michael"

document.write(Y); //This prints the variable Y on the page

var bball = "LeBron", soccer = "Messi", football = "Brady"; //Creates multiple variables at once, with different string values attached

document.write(bball); //This prints the variable bball on page

var sum = 3+5; //variable called sum with value of 3+5

document.write(sum); //prints the variable sum (8)
