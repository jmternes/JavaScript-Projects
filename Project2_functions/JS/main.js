function secondFunction () { //Creating a function called secondFunction
    var sentence = "JavaScript"; //Creating a variable called sentence with string value "JavaScript"
    sentence += " is a bit tricky at first!"; //+= Operator to concatenate the two strings
    document.getElementById("Concatenate").innerHTML = sentence; //Returns an element with same ID as the paragraph element in HTML file
}
